# /etc/systemd/system/linux-voice-assistant.service.d/10-tuning.conf
[Service]
# Keep mpv logs at warning by default; override with environment if desired
Environment=LVA_MPV_MSG_LEVEL=all=warn

# Faster, cleaner shutdowns
TimeoutStopSec=5s
KillMode=mixed
